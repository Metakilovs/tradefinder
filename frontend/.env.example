# Copy this to a file named .env in the frontend folder.
# While developing locally, leave it as-is or just delete the file (it defaults to localhost).
# Once your backend is deployed (e.g. on Render), set this to its live URL:
VITE_API_URL=https://your-backend-name.onrender.com/api
